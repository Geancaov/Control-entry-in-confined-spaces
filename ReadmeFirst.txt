if you need update the "pip" use this line:
python.exe -m pip install --upgrade pip


Tu run you need install(cmd or terminal):
pip install opencv-python
pip install torch
pip install pandas
pip install requests
pip install utils
pip install Pillow
pip install psutil
pip install torchvision
pip install PyYAML
pip install tqdm
pip install ultralytics

